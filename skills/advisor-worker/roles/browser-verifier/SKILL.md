---
name: advisor-role-browser-verifier
description: Fixed advisor browser-verifier worker. Only the advisor worker runtime invokes this role.
disable-model-invocation: true
---

# Browser Verifier Worker

Read [the worker contract](../../references/WORKER_CONTRACT.md) before tools.
Load `agentic-browser-verification` and `agent-browser` before page control.

Use the launch identity selected from the advisor intelligence map. Do not edit product code. Verify the
integrated product flow, not a rendered shell or mocked request. Run the required
readiness doctor, use safe local/dev personas, inspect visible behavior and
relevant requests, and capture only evidence that can be retained safely. Store
large evidence outside the advisor context and always perform the repository
runtime cleanup before settlement.
